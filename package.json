{
  "name": "star-astrology-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Astrology platform for personalized birth charts and transit reports",
  "workspaces": [
    "packages/shared",
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/backend && npm run build -w packages/frontend",
    "lint": "npm run lint --workspaces",
    "format": "prettier --write \"packages/*/src/**/*.{ts,tsx}\"",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test:unit -w packages/backend",
    "test:integration": "npm run test:integration -w packages/backend",
    "test:accuracy": "npm run test:accuracy -w packages/backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
